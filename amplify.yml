version: 1
frontend:
  phases:
    preBuild:
      commands:
        -  npm install --legacy-peer-deps
    build:
      commands:
        - npm run dev
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*